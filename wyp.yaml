scripts:
  install:
    root: true
    help: run Go install
    run: go install .
  test:
    help: run tests
    run: go test ./...
  message:
    env:
      - GREETING=World
    run: echo "Hello $GREETING!"
