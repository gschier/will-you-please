scripts:
  install:
    root: true
    help: run Go install
    run: go install ./...
  test:
    root: true
    help: run tests
    run: go test ./...
  vendor:
    help: refresh all dependencies
    run: go mod tidy && go mod vendor
